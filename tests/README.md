# 声纹识别评估测试

## 概述

`eval_speaker.py` 用于评估 Stage 1 音频处理 pipeline 的声纹识别质量。以人工标注的 Ground Truth (GT) 为基准，通过时间重叠匹配计算多项指标。

**核心原则：仅统计主角团角色。** CAR/SAR 的分母只包含主角团 GT 段，非主角团（如 歌手、鳄鱼妈妈）不参与计算。

## 评估指标

### CAR (Character Attribution Rate) — 角色归属正确率

```
CAR = 主角团正确识别的 GT 段数 / 主角团 GT 段数
```

**算法：**

1. 筛选 GT 中属于主角团的台词段
2. 对每个主角团 GT 段，遍历所有预测段，计算时间重叠
3. 对每个有重叠的预测段，计算「文本贡献量」= `重叠比例 × 文本长度`
4. 按角色累加贡献量，取最大者作为该 GT 段的预测角色
5. 与 GT 角色比较，一致则记为正确

**适用场景：** 评估 pipeline 对「谁在说话」的整体判断能力，容许段落切分不完全对齐。

### SAR (Speaker Attribution Rate) — 逐段匹配准确率

```
SAR = 主角团逐段正确匹配数 / 主角团 GT 段数
```

**算法：**

1. 筛选 GT 中属于主角团的台词段
2. 对每个主角团 GT 段，找到时间重叠最大的预测段
3. 直接比较 `speaker_pred` 与 `speaker_gt`

**适用场景：** 评估逐段对齐时的识别精度，对段落切分更敏感。

### 情感准确率

对有 `emotion_gt` 的全部 GT 段（含非主角团），找到时间重叠最大的预测段，比较 `emotion` 字段。

### 覆盖率

有 `speaker_pred` 的预测段占总预测段的比例。

### 声纹置信度阈值 (`--vp-threshold`)

声纹识别返回的 `vp_score` 表示匹配置信度。当低于设定阈值时，预测角色标记为「路人」：

- **0 (默认)：** 不过滤，使用 pipeline 原始输出
- **0.3~0.5：** 根据实验确定，过滤低置信度的错误归属

主角团角色（声纹库中注册的角色）：

```python
MAIN_CHARACTERS = {"喜羊羊", "灰太狼", "美羊羊", "懒羊羊", "沸羊羊", "慢羊羊"}
```

## 数据依赖

```
data/
├── output/{video}/audio.json        # Pipeline 输出 (必需)
└── gt/
    └── xiyangyang052_gt.json        # 统一 GT (声纹+情感)
```

### audio.json 格式

Pipeline Stage 1 的输出，需包含 `segments` 数组：

```json
{
  "segments": [
    {
      "begin_time": 99.4,
      "end_time": 112.1,
      "speaker_pred": "美羊羊",
      "vp_score": 0.65,
      "text": "啊？喜羊羊，时候不早了...",
      "emotion": "开心"
    }
  ]
}
```

### GT 格式 (统一文件)

每个视频一个 GT 文件，同时包含 `speaker_gt` 和 `emotion_gt`：

```json
[
  {
    "segment_id": "7b2893ed",
    "begin_time": 99.4,
    "end_time": 112.16,
    "speaker_gt": "美羊羊",
    "text": "啊？喜羊羊，时候不早了，我们还是回去吧。",
    "emotion_gt": "关切语调"
  }
]
```

GT 文件命名规则：`data/gt/{系列名}{集数}_gt.json`，通过脚本内的 `GT_FILE_MAP` 映射。

## 用法

```bash
# 基本评估
python -m tests.eval_speaker --video "052 鸟蛋之争"

# 指定声纹置信度阈值
python -m tests.eval_speaker --video "052 鸟蛋之争" --vp-threshold 0.4

# 输出详细报告 (每段 GT 的匹配详情)
python -m tests.eval_speaker --video "052 鸟蛋之争" --detail

# 保存报告到 tests/results/
python -m tests.eval_speaker --video "052 鸟蛋之争" --save
```

## 输出示例

```
======================================================================
声纹识别评估报告: 052 鸟蛋之争
======================================================================

Pipeline 输出: 114 段, 声纹预测: 107 (93.9%)
GT 总段数: 89, 主角团: 86, 其他: 3

指标                              值                   公式
----------------------------------------------------------------------
CAR (角色归属率)            96.4%    81/84    (主角团)
SAR (逐段匹配率)            92.9%    79/85    (主角团)
情感准确率                  41.9%    36/86

按角色统计 (CAR, 仅主角团):
  角色             正确      CAR
  ----------------------------
  灰太狼         32/32   100.0%
  喜羊羊         22/24    91.7%
  慢羊羊         15/16    93.8%
  沸羊羊          5/5    100.0%
  美羊羊          5/5    100.0%
  懒羊羊          2/2    100.0%

按时长统计 (CAR, 仅主角团):
  时长           正确      CAR
  --------------------------
  <2s        5/5    100.0%
  2-5s      38/41    92.7%
  5-10s     29/29   100.0%
  >10s       9/9    100.0%
======================================================================
```

## 保存的报告格式

使用 `--save` 保存到 `tests/results/eval_{video}.json`：

```json
{
  "video": "052 鸟蛋之争",
  "vp_threshold": 0.0,
  "metrics": {
    "car": 0.9643,
    "sar": 0.9294,
    "emotion_accuracy": 0.4186
  },
  "by_speaker": {
    "喜羊羊": { "total": 24, "correct": 22 },
    "灰太狼": { "total": 32, "correct": 32 }
  },
  "by_duration": {
    "<2s": { "total": 5, "correct": 5 },
    "2-5s": { "total": 41, "correct": 38 }
  },
  "car_details": [...],
  "sar_details": [...]
}
```

## 参数说明

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `--video` | str | (必需) | 视频目录名，对应 `data/output/{video}/` |
| `--vp-threshold` | float | 0.0 | 声纹置信度阈值，低于此值标为「路人」 |
| `--detail` | flag | false | 输出每段 GT 的匹配详情 |
| `--save` | flag | false | 保存 JSON 报告到 `tests/results/` |

## 细粒度分析

### 按角色统计

展示每个主角团角色的识别正确率，用于发现特定角色声纹注册不足或声纹相似导致的混淆。

### 按时长统计

将 GT 段按时长分为 4 个区间 (`<2s`, `2-5s`, `5-10s`, `>10s`)，分析不同时长段的识别差异。
